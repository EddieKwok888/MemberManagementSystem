{
  "name": "member-system-backend",
  "version": "1.0.0",
  "description": "Production-ready Firebase backend for Member System",
  "scripts": {
    "bootstrap": "ts-node scripts/bootstrap.ts",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.9.0",
    "firebase-admin": "^11.8.0"
  }
}
